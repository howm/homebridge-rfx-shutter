{
  "name": "@howm/homebridge-blind",
  "version": "0.1.0",
  "main": "src/index.ts",
  "repository": "git@github.com:howm/homebridge-blind.git",
  "author": "howm",
  "description": "Homebrdige plugin for blinds controlled by RFXtrx433(E) transceivers.",
  "license": "MIT",
  "private": false,
  "scripts": {
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "build": "buildTSPackage",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@boilerz/build-tools": "^1.3.0",
    "@boilerz/eslint-config": "^1.5.0",
    "@boilerz/prettier-config": "^1.0.0",
    "@boilerz/tsconfig": "^1.3.0",
    "@types/jest": "^24.0.17",
    "eslint": "^6.1.0",
    "jest": "^24.8.0",
    "prettier": "^1",
    "ts-jest": "^24.0.2",
    "typescript": "^3"
  },
  "greenkeeper": {
    "commitMessages": {
      "initialBadge": ":pencil: add Greenkeeper badge",
      "initialDependencies": ":arrow_up: update dependencies",
      "initialBranches": ":tada: whitelist greenkeeper branches",
      "dependencyUpdate": ":arrow_up: update ${dependency} to version ${version}",
      "devDependencyUpdate": ":arrow_up: update ${dependency} to version ${version}",
      "dependencyPin": ":pushpin: pin ${dependency} to ${oldVersion}",
      "devDependencyPin": ":pushpin: pin ${dependency} to ${oldVersion}"
    }
  }
}
